[package]
name = "decentraai-fabric"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
uuid.workspace = true
# WorkerFacts carries the on-disk model set (Vec<ServedModel>) so the planner
# can discover what a worker COULD serve, not just what is loaded. ServedModel
# is a pure serde-serializable type; no I/O enters this crate.
decentraai-compute = { path = "../compute" }
decentraai-hub = { path = "../hub" }

[dev-dependencies]
tempfile.workspace = true