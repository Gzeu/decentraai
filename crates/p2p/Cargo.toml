[package]
name = "decentraai-p2p"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
async-trait.workspace = true
blake3.workspace = true
decentraai-audit = { path = "../audit" }
decentraai-identity = { path = "../identity" }
decentraai-manifest = { path = "../manifest" }
decentraai-protocol = { path = "../protocol" }
# Timeout alignment: the P2P request/response budget must derive from the
# SAME shared value as the backend HTTP client and remote routes, otherwise
# the shortest layer cuts healthy slow-prefill workers mid-generation.
decentraai-config = { path = "../config" }
decentraai-registry = { path = "../registry" }
futures.workspace = true
libp2p.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
uuid.workspace = true

[dev-dependencies]
tempfile.workspace = true
