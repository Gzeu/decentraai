# EXAMPLE ONLY — no final token parameters are chosen. Edit this file to run different scenarios; code never changes.

Closed schema: unknown fields are rejected by design (deny_unknown_fields). Documentation lives in docs/TOKENOMICS.md.
